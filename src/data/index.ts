/**
 * Data layer: re-export all copy from data.ts so consumers can do
 * import { site, header, hero, footer, book, ourStory, a11y } from "@/data"
 */
export { site, header, hero, footer, book, ourStory, a11y } from "./data";
